import {requirePagePermission} from "@/lib/permissions"; import {getContract} from "@/lib/offers-contracts"; import {getAllCompanies,getLegalEntities,getProjects} from "@/lib/data"; import {ContractForm} from "@/components/commercial/contract-form";
export default async function EditContract({params}:{params:Promise<{id:string}>}){await requirePagePermission("contract.update");const d=await getContract((await params).id);if(!d.contract)return null;return <><h1 className="h3 mb-3">Modifica contratto</h1><ContractForm record={d.contract} entities={await getLegalEntities()} companies={await getAllCompanies()} projects={await getProjects()}/></>}
