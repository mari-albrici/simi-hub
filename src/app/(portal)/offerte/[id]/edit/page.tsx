import { requirePagePermission } from "@/lib/permissions"; import {getOffer} from "@/lib/offers-contracts"; import {getAllCompanies,getLegalEntities,getProjects} from "@/lib/data"; import {OfferForm} from "@/components/commercial/offer-form";
export default async function EditOffer({params}:{params:Promise<{id:string}>}){await requirePagePermission("offer.update");const d=await getOffer((await params).id);if(!d.offer)return null;return <><h1 className="h3 mb-3">Modifica offerta</h1><OfferForm record={d.offer} entities={await getLegalEntities()} companies={await getAllCompanies()} projects={await getProjects()}/></>}
