import { statusMeta,type StatusDomain } from "@/lib/status";
export function StatusBadge({domain="document",status,variant}:{domain?:StatusDomain;status:string;variant?:"success"|"warning"|"danger"|"primary"|"secondary"|"info"}){const meta=statusMeta(domain,status);return <span className={`badge text-bg-${variant??meta.variant}`}>{meta.icon&&<i className={`bi bi-${meta.icon} me-1`} aria-hidden="true"/>}{meta.label}</span>;}
